# M2.6 Real-Model Blind Research Gate Implementation Plan

> **For agentic workers:** Inline execution is selected for this repository. Work task-by-task with fresh tests and checkpoints.

**Goal:** Run a three-round, nine-scenario local blind benchmark through the real OpenAI-compatible research path and produce auditable TP/FP/FN, cost, token, and scope metrics.

**Architecture:** Keep `LocalLabExecutor` strictly local. Add benchmark ground truth only inside fixture/metric code, create sanitized model-facing profiles, and persist each run/case result separately from Findings. The deterministic final gate remains the only path to `SUBMISSION_READY`.

**Tech Stack:** Python 3.12, Pydantic 2, Typer, SQLite Repository, FastAPI/httpx ASGI transport, pytest, Ruff.

---

### Task 1: Add experiment domain records and storage

**Files:**
- Modify: `src/ai_bug_bounty/domain.py`
- Modify: `src/ai_bug_bounty/storage.py`
- Test: `tests/test_mvp.py`

- [x] Add strict `ExperimentRun`, `ExperimentCaseResult`, and `ExperimentSummary` models with scenario class, run number, finding state, TP/FP/FN, scope violations, token usage, cost, and ground-truth-free persisted model context hash.
- [x] Add repository save/list/summary helpers using the existing entities table and deterministic aggregation.
- [x] Add tests for SQLite reload and summary counts.

### Task 2: Expand the local benchmark to nine scenarios

**Files:**
- Modify: `src/ai_bug_bounty/lab.py`
- Modify: `src/ai_bug_bounty/programs.py`
- Test: `tests/test_mvp.py`

- [x] Add three deceptive safe endpoints: public profile, explicitly shared document, and non-sensitive `internal_id` response.
- [x] Keep fixture ground truth in the executor/metric boundary only; expose sanitized descriptions and operations in the public profile.
- [x] Add tests proving the executor returns safe observations and still rejects every non-`lab://` target.

### Task 3: Remove oracle fields from model-facing context

**Files:**
- Modify: `src/ai_bug_bounty/lab.py`
- Modify: `src/ai_bug_bounty/workflow.py`
- Modify: `src/ai_bug_bounty/providers.py`
- Test: `tests/test_mvp.py`

- [x] Build a public profile projection containing only policy, scope, public brief, API/feature descriptions, and test accounts.
- [x] Ensure OpenAI-compatible Planner/Validator/Skeptic/Impact payloads contain no scenario labels, expected statuses, fixture function names, or prior findings.
- [x] Add a test that serializes provider context and asserts no ground-truth marker appears.

### Task 4: Add three-round experiment orchestration and metrics CLI

**Files:**
- Create: `src/ai_bug_bounty/experiments.py`
- Modify: `src/ai_bug_bounty/cli.py`
- Test: `tests/test_mvp.py`

- [x] Implement fresh-run orchestration with a new provider context per round, shuffled scenario order, isolated hypotheses, and exactly two reproductions per case.
- [x] Classify outcomes as TP/FP/FN from fixture truth plus final Finding state; count scope violations and gate failures.
- [x] Add `experiment-run` and `experiment-summary` commands with `--provider` and `--rounds` controls; default rounds to 3 and block non-local assets.
- [x] Add deterministic-provider tests for 27 case-runs and exact metric aggregation.

### Task 5: Validate real provider path and cost accounting

**Files:**
- Modify: `src/ai_bug_bounty/providers.py`
- Modify: `src/ai_bug_bounty/cost.py`
- Test: `tests/test_mvp.py`

- [x] Keep model network disabled unless `ABB_LLM_NETWORK_ENABLED=true` and API key/base URL/model are present.
- [x] Ensure planner, validator planner, skeptic, and impact reviewer each record usage and prices; missing values remain UNKNOWN.
- [x] Add mocked OpenAI-compatible contract/prompt tests proving no target network calls and no oracle context.
- [ ] Run a real endpoint smoke test with configured credentials; this remains an explicit external prerequisite.

### Task 6: Documentation, CI, and gate verification

**Files:**
- Modify: `README.md`
- Modify: `docs/ACCEPTANCE.md`
- Modify: `.github/workflows/ci.yml`

- [x] Document the nine-scenario experiment, metrics, model configuration, and explicit non-goals.
- [x] Run pytest, Ruff, compileall, pip check, and fresh CLI experiment-summary verification.
- [x] Record that the offline surrogate gate passes while the Real-Model Gate remains NOT VERIFIED without credentials.
